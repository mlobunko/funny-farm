import * as t from './actionTypes';

export const playSound = () => ({
  type: t.PLAY_SOUND
});

export const playingOn = () => ({
  type: t.PLAING_ON
});

export const playingOff = () => ({
  type: t.PLAYING_OFF
});
